This project demonstrates a basic CI pipeline using GitHub Actions for a Java Maven application.


Builds a Java application using Maven
Caches dependencies for faster builds
Creates a JAR file
Uploads the JAR as an artifact
Downloads the artifact in another job
